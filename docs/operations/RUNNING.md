# 本次交付运行记录

核对时间：2026-09-06T02:36:03.042277+08:00

- 默认离线应用：`http://127.0.0.1:8765/`，真实health检查ok，后台worker无错误。启动命令`./software/start.sh`；本次私有角色链接位于权限0600的`.local/demo-server.log`，终端可查看，资料服务器禁止访问该路径。
- 资料中心：`http://127.0.0.1:8766/`，已在用户Chrome打开最终页；8项隔离浏览器检查通过，10个视频均取得可播放元数据，桌面/移动截图已保存。
- 一次性合成QA服务18765已停止，内存数据库销毁、私有令牌文件清理。
- 所有软件、硬件、PPT及修复CLI均已正常完成，未遗留付费模型或视频生成进程。会话ID与退出码见`CLI_SESSIONS.json`。
- 本地监督器已收到STOP，停止监视；没有执行真实故障恢复、额度重置、购买或GPU租赁。十小时是本次工作上限，未声称连续运行十小时或自动恢复保证。

应用和资料中心为本机进程，重启电脑后需要重新执行启动命令。不改系统开机启动、代理或用户浏览器保护。当前Chrome配置对8765的拦截曾存在；可直接查看真实软件录屏与测试证据，未为此关闭保护。

复现浏览器验收：先执行`python3 scripts/browser_qa_server.py`，再执行`scripts/verify_browser.mjs`与`scripts/verify_model_ui.mjs`（Node+Playwright环境定位见脚本）；这两项不调用第三方模型。模型实时测试另有显式开关，当前剩余1/20调用，不自动补充。
